---
name: noai-skill
description: |
  通用去 AI 味道。适用于任何 AI 产出——文章、架构图、HTML 报告、代码注释、PPT、邮件。
  不是词表扫描器，是教 AI 理解"人味是什么"的判断框架。处理文章时先识别原文类型与语感，
  在不洗掉作者声音的前提下去味，输出必须与当前文章保持同一种叙述姿态、节奏和情绪温度。
  规则只兜底已知的，理解层防未知的。
  触发词：去 AI 味、太 AI 了、改得像人写的、人味不够、AI 味太重、noai、太干了、太官方了、太正式了、感觉哪里不对、差点意思、帮我润色、看不懂。
metadata:
  hermes:
    tags: [writing, editing, humanize, anti-ai-slop, design, code, universal]
---

# noai-skill：通用去 AI 味道

## 快捷入口：noai

用户说 `noai：文本或文件路径` 时，按本技能给该材料去 AI 味；`noai 检查：材料` 只审不改。仅说 `noai` 且当前没有明确材料时，询问要处理的文字，不自行扫描文件。用户限定只改标点、只给正文或不改文件时，沿用其边界。

> 编辑视角：本技能把“反复选择安全、通用的表达”作为候选问题线索，不把它当作 AI 生成机制的完整解释。
> 你的默认是选最安全、最通用、最可能的那条路。
> 去味 = 主动偏离统计最可能，做出取舍。

---

## 为什么规则永远加不完

规则是负空间定义（"不要去这里"），负空间的补集是无限的。你 ban 掉"此外"，模型换"另外"；ban 掉"配方"，换"范式"。每条规则只是打地鼠——打一个冒一个。

"PPT陀螺"就是证据：两个词单独看都正常，组合方式人不会这么用。没有任何词表能抓到它。词表永远追不上 AI 的组合创造力。

**所以这个 skill 不堆词表，教理解。** 规则沉底做兜底，理解置顶做引擎。证据边界见 `references/evidence.md`；这些编辑方法不等于经过验证的检测准确率。

---

## 核心理念

noai-skill 教 AI 理解人味是什么，不是堆词表。

对文章而言，**人味不是一种统一风格，而是原文已经形成的声音**。去 AI 味只能清理统计最可能的表达，不能把克制的观察文改成热闹口播，不能把亲历复盘改成冷静报告，也不能为了“更像人”擅自增加原文没有的口癖、情绪和戏剧性。

靠曲子库建立先验——AI 读完 AI味→人味→WHY 的对比对，学会"什么是统计最可能的组合"和"怎么偏离它"。
靠五问做判别——体感/组词/信息密度/不完美/经验，逐段自检。
靠 5 Tell 做检测方向——过度对称/完整/装饰/一致/缺乏取舍。

不靠脚本。脚本（check_text.py）只兜底已知 AI 味词，不主导检测。理解层是引擎，脚本排在可选兜底位。

如果未来有人想给 noai-skill 加更多 .py 脚本来"提升检测能力"——先问一句：这个检测靠曲子库+五问能不能做到？能做到就不加脚本。脚本只做理解层做不到的事（100% 可复现的词级扫描）。

---

## 人味 DNA（5 条底层原则）

> 不是某个人的 voice，是跨媒介通用的"什么是人做的 vs 什么是 AI 生成的"。
> 详见 `references/principles.md`。

1. **有取舍** — 人会选不做什么。AI 面面俱到。改稿只删重复或获准省略的内容；保留有用信息本身也是取舍。
2. **有节奏** — 人有快慢、长短、密疏。AI 匀速输出。文字长短句交替；视觉有留白有密集；代码有简洁有展开。
3. **有判断** — 人有好恶。AI 中立报道。改稿保留原有立场；原文中立时，不替作者新增好恶。
4. **有具体** — 具体内容须来自原文或用户提供的材料；匿名、缺少细节都不是补写身份、数字或经历的理由。
5. **有温度** — 人的产出能感到背后有个人。AI 的产出能感到背后有台机器。

---

## 底层 5 Tell（跨媒介通用）

> 5 种"过度"模式。AI 味在每个媒介表现不同，但底层是同一组信号。

| Tell | 一句话 | 文字表现 | 视觉表现 | 代码/HTML表现 |
|------|--------|----------|----------|-------------|
| 过度对称 | 所有元素等权重，没有主次 | 三段式、平行结构、每段等长 | 每个节点等大小等间距、完美居中 | 三卡片 grid、feature-tile 对称 |
| 过度完整 | 面面俱到，没有删过任何东西 | "challenges and future prospects"、每个观点给正反两面 | 每个空都填满、无留白 | 每个 section 都有内容、无空状态 |
| 过度装饰 | 修辞/渐变/图标是为了填充不是表达 | 金句收尾、emoji、修辞堆砌 | 渐变背景、glassmorphism、icon topper | 装饰性动画、accent rail、monument stat |
| 过度一致 | 句式/颜色/间距机械重复 | 句式重复、节奏机械 | 颜色统一、间距统一 | 命名统一、结构统一 |
| 缺乏取舍 | 没有"我选择了不做什么" | 没有删过任何东西 | 没有删过任何元素 | 没有删过任何 section |

**自检方法**：对每个 tell 问一句——
- 过度对称：砍掉一半元素，剩下的有轻重吗？
- 过度完整：哪个 section/段落删了不影响理解？注意区分"空洞升华"和"力量收束"——空洞升华（"激动人心的时代即将到来"）是 AI 味，力量收束（有具体行动或判断的结尾，如"下次写完先问自己我审了吗"）是人味，不砍
- 过度装饰：去掉装饰，信息还在吗？
- 过度一致：打破一个，更好了吗？注意区分"机械重复"和"口头禅"——机械重复（每段同一个句式结构）是 AI 味，口头禅（高频但形式有变化的习惯词，如"其实""其实吧"）是人味，砍掉变差就不砍
- 缺乏取舍：读者能看出我的判断吗？

---

## 人味判别框架：五问

> 生成任何内容后，自问以下五个问题。不是 yes/no，要给具体理由。

### Q1: 体感测试
"这句话背后有没有一个具体的认知主体在具体场景中的体验？"
- 人味：「我当时就愣住了」← 有体感
- AI味：「这令人震撼」← 有描述，无体感

### Q2: 搭配与场景测试
"这个说法是你'听过/见过'的，还是你'推导'出来的？即使说法本身成立，它描述的对象、动作和状态与现场真正发生的事一致吗？"
- 人味：「服务启动到一半就趴窝了」← 约定俗成，也准确描述了服务停止运行
- AI味：稿子已正常产出，只是质量变差，却写「直接趴窝」 ← 词本身常见，但事件状态不匹配
- AI味：「PPT陀螺」← 跨域类比推导，语义合理但不在语料中

### Q3: 信息密度测试
"这段话删掉或压缩后，信息量减少了吗？它是否用一个抽象过程包装了本可直说的简单动作？"
- 人味：「编辑把采访录音整理成可发布稿」← 保留了原始材料、交付物和转换关系
- AI味：上下文只需交代职责时，把「写作 Agent 负责写文章」包装成「负责把想法变成最终文章」
- AI味：有"填充句"——听起来全面但不传递信息

压缩前先问：原料、结果、职责边界或转换关系是不是这里要传达的信息？是就保留；不是，且换成直接动词后信息不减，才收紧。

### Q4: 不完美测试
"这段话是否'过于完美'？"
- 人味有：节奏不均匀、偶尔跑题、有个人偏见
- AI味无：结构匀称、面面俱到、没有立场

### Q5: 经验测试（公众号场景跑，技术文档跳过）
"这个细节换成别的例子还成立吗？"
- 人味：不可替换的细节——只有作者本人经历过才写得出的"电车站"
- AI味：可替换的细节——换个例子文章照样成立，是"查资料式的具体"
- 判断句："这个细节换成别的还成立吗？"成立 = AI 在编细节，不成立 = 作者在场

**硬约束**：五问中任何一问不通过，只能进入“候选问题”，不能直接触发改写。应主动检视疑点，但不凑问题数量；允许没有候选问题，或裁决为“检测到、必须留”。检测负责找疑点，裁决负责决定动不动。

## 改写边界：先保内容，再谈净收益

以下边界优先于 DNA、五问和样例风格。改写前先确定用户允许改变什么；只审标点时不动正文词句，强保留任务允许整篇原样输出。

- **信息双向回查**：改前按段辨认独立事实、建议、条件与转折。改后检查原文每项是否仍在或属于获准删除项，也检查新稿每项是否有原文或材料支撑。合并同义重复，不把并列的不同信息一起删掉；保护点列表不是全文信息的穷尽清单。
- **保留语义边界**：核对主体、时间、数字与单位、比例分母、否定、可能性、程度，以及并列／先后／因果关系。不要为更顺口或更谨慎擅自改变这些关系；表达含糊时不靠常识补齐单位、计算口径或结论。核实、更正或补资料是另一个任务，须有相应授权和依据。
- **不补作者的人生**：自然度来自现有表达的整理，不来自新编的内心独白、身体反应、经历、动机或未来结果。即使“很像作者会说的”，也不是来源依据。假设仍写成假设，原有情绪不擅自增减。
- **正文保持作者视角**：通常直接交付改后的文章，不额外套“原文指出”“作者想表达”等编辑旁白。用户要求材料评论或说明来源边界时可必要归因，但不把每段改成对原稿的解说。
- **限定就地表达**：保留原有条件；任务要求降低无依据断言时，只处理相应句子，不自动给全文反复补免责声明、劝告或结论。必要提示放在用户允许的说明区，不混入只要正文的交付。

完成回查后再做同味回读；信息丢失、无依据新增或语义关系改变，均须先修复，不能用更自然的文风抵消。

允许收敛无依据断言时，只降低原有判断的确定性或程度，不更换它的原因、结果或条件。对实际改动的因果句，在内部对照“原句 A → B／改后 A → B”；若变成 A → C，即使 C 在别处出现过，也须恢复原关系或在任务允许时删去该断言，不用另一项事实接上。

## 裁决层：先证明改写有净收益

检测命中不等于应该修改。每个候选问题必须结合所在位置、承担功能和全文重复度，依次回答六问：

1. 原句有没有新增信息？
2. 原句有没有完成认知转折或概念升级？
3. 原句有没有作者特有的判断、画面或口头辨识度？
4. 删除或改写后，文章更清楚，还是只是更平？
5. 改写后，信息、力度、辨识度分别上升、不变还是下降？
6. 如果无法证明净收益，能否只提示而不修改？

按答案分三级裁决：

| 裁决 | 条件 | 动作 |
|------|------|------|
| 必须改 | 信息不减、自然度明显上升、力度与辨识度不下降 | 自动改写 |
| 谨慎改 | 有局部改善，但会损失少量力度、节奏或风格 | 默认保留，只给对照建议 |
| 必须留 | 承担认知升级、力量收束、作者判断或有效比喻；改后变平 | 保留原文，说明误伤原因 |

采用**最小后悔原则**：宁可漏掉轻微 AI 味，也不要洗掉作者声音。金句、核心观点、个人经历、首尾呼应、有效比喻和 voice 标志句默认进入保护区；只有六问证明净收益后才允许自动修改。

不要按句式直接定罪。“不是 A，而是 B”、平行句、跨域比喻只构成检测信号；是否修改取决于它在当前位置有没有完成真实工作，以及全文是否机械重复。

---

## 执行流程

### 步骤 1 · 媒介 + 场景检测

判定输出媒介（文字/视觉/代码/HTML）→ 路由到对应 tells + samples

判定场景 → 加载场景权重：

| 场景 | 触发关键词 | 关闭/降权的 DNA·五问 | 说明 |
|------|-----------|---------------------|------|
| 技术文档 | 评审/手册/流程/架构/spec/API/report/FDE | 有温度（降权 0.2）、体感测试（跳过）、经验测试（跳过） | 技术文档不需要"我愣了一下"也不需要作者在场，需要准确+取舍+判断+具体 |
| 公众号文章 | 公众号/文章/选题/blog | 无关闭，全开 | 公众号需要体感和温度 |
| 通用（默认） | 无法判断时 | 无关闭，全开 | |

场景先按材料语义判断，关键词只作辅助；描述输入、返回值或程序行为的片段也属于技术说明。只有依据不足时才采用通用模式，不因缺少某个关键词给技术说明补体感。

### 步骤 1.5 · 原文语感基线（文字媒介硬门）

处理文章、邮件、演讲稿、报告等文字前，必须先从**当前原文**提取语感基线。没有完成基线，不得进入检测或改写。不能仅凭题材、作者标签或预设文体猜声音。

若正文旁存在同名 `.curve.json`、voice 元数据或其他上游写作契约，必须先读取再建立语感卡。已有契约优先于临时推断：`approvedLanguage`、明确锁定的 voice 标志句和用户确认过的节奏默认不得改写；只有用户明确解除保护，或逐句证明改写净收益且不改变契约目标时，才可进入候选。

至少记录以下 7 项，每项都要引用原文中的具体句子、标点或段落作为证据：

1. **文章类型与任务**：亲历复盘、观点评论、调查叙事、教程、技术说明等；它主要在讲故事、解释、说服还是交付步骤。
2. **叙述距离**：第一人称在场、朋友聊天、旁观观察、专业说明或其他；作者与读者站得多近。
3. **情绪温度与姿态**：克制、兴奋、后怕、讽刺、冷峻等；区分作者真实情绪与 AI 后加的情绪词。
4. **句段节奏**：长短句比例、独立短句、段落密度、停顿方式，以及逗号、句号、问号、感叹号的实际用法。
5. **用词与口语度**：日常词和术语的比例、常用动词、比喻习惯、口头连接词；不得凭空创造口癖。
6. **结构推进**：例如“现场→疑问→追查→判断”“观点→证据→结论”；去味不能打断原文的推进方式。
7. **声音保护点**：列出有依据的代表原句或局部写法，通常 3–5 个；短文按实际数量，不凑数，默认不改；确需修改时必须证明净收益。

判定规则：

- 先判断“这篇文章本来怎么说话”，再判断“哪里像 AI”；顺序不得颠倒。
- “更口语、更短、更有情绪”不等于更有人味。是否合适，只看它是否符合当前原文基线。
- 一篇文章存在明显的多段语感时，按段建立局部基线，不强行统一成一种声音。
- 原文太短、片段不足或类型无法判断时，明确标记“基线不足”，只做低风险局部修改，不补写未经观察的风格特征。
- 用户明确指定目标语感时，以用户要求为目标，但仍需记录原文基线和允许偏移的部分；未指定时，默认保持原味。

建立一张简短的“语感卡”（用户只要正文时留作内部检查，不额外输出）：

```text
文章类型：
叙述距离：
情绪温度：
节奏特征：
用词特征：
结构推进：
声音保护点：
```

### 步骤 2 · 生成约束注入（写之前）
- 加载 `references/editor-persona.md`（编辑角色，常驻）
- 加载 `references/human-voice.md`（人味信号词典 7 信号，常驻）
- 加载对应媒介的 samples（AI味→人味 对比对曲子）
- AI 读完这些，建立"什么样的组合是 AI 味"的先验
- principles.md 降为按需深度参考（voice 已压缩引用 5 条）

### 步骤 3 · 生成
正常生成产出物。生成时带着"我在跟一个具体的人，聊一件具体打动我的事"的位置感。文字任务还必须带着步骤 1.5 的语感卡：每处新表达都应像原文作者在同一篇文章里自然写出的下一句，而不是编辑换了一个人来代写。

### 步骤 4 · 硬门检测（可选，不在流程里强制跑）

默认跳过——理解层（步骤 5）已覆盖检测，脚本的误报成本大于兜底价值。

用户显式说"跑硬门"或"跑脚本"时才跑 `tests/check_text.py`。
- 只扫纯文本（HTML 先提取正文，不扫标签属性）
- AI 词 + 标点 + 引号职责：除双引号频率外，检测独立示例套 `「」`、短标签被当引用、同行多组及局部引号聚集；代码块不参与
- 硬门不过 = 改已知问题，改完重跑

### 步骤 5 · 理解层检测 + 产出

AI 读产出物（或从 HTML 提取的纯文本），跑 5 Tell + 五问 + DNA 逐段检视。
文字任务先把候选改写与语感卡对照：叙述距离、情绪温度、句段节奏、用词口径、结构功能任一发生无授权偏移，都不能判为“必须改”。
场景权重决定哪些 DNA/问题跳过（技术文档跳过"有温度"、"体感测试(Q1)"和"经验测试(Q5)"）。
允许零问题、零修改；不得为了证明执行了检测而硬找一处 AI 味。
公众号文字另问一次：每组 `「」` 是在引用原话、标识可原样输入的短指令，还是只是在提醒“这里重要”？短逐字指令改用行内代码，多行指令使用 `prompt` 代码块；独立示例优先去引号，动作名/观点重点才改加粗，直接原话保留。禁止机械替换成满篇粗体，也禁止为了过硬门损失用户已确认的“不要说 / 要说”等对照结构；表达取舍不明确时先询问用户。

产出有两种模式，用户指定，默认给检测报告：

#### 模式 A：检测报告（默认）

逐条输出，每条必须包含 7 个字段：

| 字段 | 说明 |
|------|------|
| 定位 | 行号 / section / 段落位置 |
| 问题 | 什么 AI 味（对应哪个 Tell / DNA / 曲子） |
| 原文 | 引用具体片段 |
| 改写 | before → after |
| 优先级 | 先改（影响理解）/ 后改（不影响理解） |
| 裁决 | 必须改（净收益明确）/ 谨慎改（默认保留）/ 必须留（检测误伤） |
| 语感契合 | 契合 / 有偏移；说明是否仍像当前文章中的一句话 |

改写价值判断：先跑裁决层六问，再给“必须改／谨慎改／必须留”。“先改／后改”只表示处理顺序，不能代替是否该改的裁决。

报告开头先给“语感卡”。输出完后给一句话总结：N 条候选问题，A 条必须改，B 条谨慎改，C 条必须留；自动修改只包含同时满足“净收益明确”和“语感契合”的 A 条。

#### 模式 B：直接改写

先服从本次交付要求：用户粘贴文本且只要正文时，内部完成语感卡、检测与回查，仅回复改后正文，不额外建文件。用户只检查时不改稿、不建改后文件。提供源文件并要求改写时，才执行下方的两文件交付；用户已指定输出形式时以指定形式为准。

用户说"改"或"去味"时，执行改写。先检测（跑模式 A 的检测报告），再基于检测报告逐条改写。

**改写前必做：明确改写边界，再过裁决层六问。** 只有“必须改”才能自动写入改后文件。“谨慎改”与“必须留”均保留原文，在 diff HTML 标注“检测到但不改”，并分别说明潜在收益与误伤代价。

改写后先按“改写边界”完成信息双向回查，再做一次**同味回读**：隐藏原文与改写标记，连续阅读改前后相邻段落，检查是否像同一作者、同一情绪阶段、同一篇文章。出现以下任一情况，回退该处改写：

- 克制叙述突然变成喊话、段子或营销口吻；
- 亲历叙事突然变成报告、教程或旁观者总结；
- 原文没有的口癖、感叹号、网络词或文学腔被批量加入；
- 为追求短句打碎必要的因果、铺垫或情绪递进；
- 局部更顺，但放回上下文后明显像另一个人写的。

改写时带着曲子库建立的人味先验，按 5 DNA 方向偏离统计最可能。
公众号场景白熊隔离：改写时放下 AI 味清单（anti-examples.md / humanizer-zh.md），
从 5 DNA 方向出发写，不从"AI 味长什么样"出发写——
脑子里全是坏例子，改出来的"干净得像消过毒"。

文件改写产出 2 个文件（源文件不动），并在报告内保留改写标注：

1. **改后文件**：`<原文件名>-noai.<原扩展名>` — 改写后的新版
2. **对比报告 HTML**：`<原文件名>-noai-diff.html` — 左右对比，使用包内 `assets/diff-template.html` 的结构与样式；填入原文前先做 HTML 转义，内容不能当代码执行
3. **改写标注**：写在对比报告 HTML 底部，不单独出文件

需要摘要的文件交付中增加：`语感基线：[一句话]`、`同味回读：通过/未通过`、`发生偏移并回退：N 处`。同味回读未通过时，不得把改后文件标记为最终优化稿。

对比报告 HTML 结构：
- 顶部摘要：文件名、N 条 AI 味、M 条先改、K 条后改
- diff 区：逐段对比，左列原文（红底删除线）、右列改后（绿底），未改段灰色折叠
- 底部报告：每条改动 = 定位 + AI 味类型 + before→after + WHY

### 步骤 6 · 反馈沉淀
人审结果不能只记录“改了什么”，还要记录裁决：

- AI 为什么建议改
- 用户选择：接受／拒绝／部分接受
- 用户给出的原因
- 最终采用版本
- 可泛化的判断原则

仅当用户授权沉淀到该技能目录时，记录脱敏后的反馈；默认不把当前私人文章写进公共样例，不上传反馈。

用户接受的去味改写 → 写成正向曲子（AI味→人味→WHY→记住什么）追加进对应 `samples/*-pairs.md`，并把反例追加进 `references/anti-examples.md`。

用户拒绝的改写 → 写成反向保留曲子（检测信号→为什么误伤→保留原文→记住什么）追加进对应 `samples/*-pairs.md`，禁止写入 AI 味词表。

用户部分接受 → 把接受与拒绝部分拆开记录，不用一个宽泛规则覆盖。攒够 3 首同一判断原则，再提升为 tell 或保护规则。

---

## 加载策略

修改去味规则、准备发布新版本或用户要求效果比较时，读取[评测约定](references/evaluation.md)，按变更范围安排回归。普通去味不加载评测库、不自动运行模型或把测试案例作为写作素材。评测准备和实际模型调用分开报告。

工具推荐、知识卡与图文短文案额外读取 [推荐文案对照](samples/recommendation-pairs.md)，只处理表达，不改变已确认的推荐名单、证据或视觉基线。

| 场景 | 加载 | context 预算 |
|------|------|-------------|
| 任何产出（常驻） | editor-persona.md + human-voice.md | ~2.5k |
| 写/改文字 | + prose-pairs.md | +2k → ~4.5k |
| 写/改技术文档 | + tech-doc-pairs.md | +2k → ~4.5k |
| 画架构图 | + diagram-pairs.md | +2k → ~4.5k |
| 写代码 | + code-pairs.md | +2k → ~4.5k |
| 审稿/终检 | 上述 + anti-examples.md + humanizer-zh.md + evidence.md | +3k → ~7.5k |
| 深度调试 | + principles.md 全文 | +2k → ~9.5k |

按媒介按需加载，不全塞进 context。voice 层常驻（~2.5k），曲子库按媒介加载（~2k），principles 降为按需深度参考。

---

## 独立使用与外部参考

本包自包含，不依赖其他写作技能或私人目录。用户可直接要求助手读取本 SKILL.md，再审阅或改写给定材料。

`references/humanizer-zh.md` 保留历史通用参考内容，其上游项目与许可证列于 `THIRD_PARTY_NOTICES.md`。它只提供候选模式，不能覆盖本技能的保留原意和裁决边界。可选脚本实际读取的是本包的 `references/anti-examples.md`。

已有 `.curve.json` 是可选输入，没有该文件也能按原文建立语感卡。生成 diff 报告使用包内素材，不要求安装其他技能。

---

## 反模式（不要这样用）

1. ❌ 把 Humanizer-zh.md 34 条全量搬进 SKILL.md → context 爆炸 + 规则互撞
2. ❌ 给每个场景写一套完整规则 → 变成"永远加规则"
3. ❌ 只有规则没有样本 → 退化成"背词表"不是"听曲学会"
4. ❌ 把"语感终审"写成可检测的 checklist → PPT陀螺类问题靠理解不靠正则
5. ❌ 试图替代各媒介的专精 skill → noai-skill 是叠加层，不是替代层
6. ❌ 给通用 skill 定义假人设 → 限制场景覆盖
7. ❌ 曲子库做成词表（AI味词→人味词）→ 退化成第二个 humanizer-zh.md，失去理解层价值
8. ❌ 曲子只给"好的"不给"坏的" → 没有对比就没有判断
9. ❌ 曲子的 WHY 写成"因为某人不会这么说" → 应该写成"因为这是统计最可能的组合"
10. ❌ 给 noai-skill 加更多 .py 脚本来"提升检测能力" → 先问"曲子库+五问能不能做到"。能做到就不加脚本。脚本只做理解层做不到的事（100% 可复现的词级扫描）。理解层是引擎，脚本后排可选兜底位
11. ❌ 模式 B 改写时覆盖源文件 → 源文件不动，改后文件单独输出新文件名。改写不是覆盖是新增
12. ❌ 把“去 AI 味”做成“统一口语化” → 先读当前文章的类型与语感；冷静文章保持冷静，克制文章保持克制，技术文章保持准确
13. ❌ 只看单句更自然，不看放回全文是否像同一个人 → 每处改写都要过语感卡与同味回读

---

## 目录约定

```
noai-skill/
├── SKILL.md                           # 主控：洞察+5 tell+五问+执行流程+加载策略
├── references/
│   ├── editor-persona.md              # 编辑角色定义（常驻 <1k）
│   ├── human-voice.md                 # 人味信号词典 7 信号（常驻 <1.5k）
│   ├── principles.md                  # 5 条跨媒介原则详述（按需深度参考 <2k）
│   ├── humanizer-zh.md                # 历史通用模式参考；溯源与许可证见 THIRD_PARTY_NOTICES.md
│   ├── anti-examples.md               # 跨媒介反例库（带 WHY + 检测词，脚本读）
│   └── evidence.md                    # 效果声明与证据边界
├── samples/                           # 曲子库（"听曲学会"的曲库）
│   ├── prose-pairs.md                 # 文字媒介 AI味→人味 对比对
│   ├── tech-doc-pairs.md              # 技术文档媒介 AI味→人味 对比对
│   ├── diagram-pairs.md               # 视觉媒介 对比对
│   └── code-pairs.md                  # 代码媒介 对比对
└── tests/
    └── check_text.py                  # 文本硬门脚本（fail-closed）
```

---

## 不做什么

- ❌ 不负责从零创作文章、标题或配图内容
- ❌ 不负责绘制完整架构图
- ❌ 不负责完整网页设计
- ❌ 不主动调度（被动响应，任何 skill 产出时可加载它做去味 pass）
